import { HangmanPartDirective } from './hangman-part.directive';

describe('HangmanPartDirective', () => {
  it('should create an instance', () => {
    const directive = new HangmanPartDirective();
    expect(directive).toBeTruthy();
  });
});
